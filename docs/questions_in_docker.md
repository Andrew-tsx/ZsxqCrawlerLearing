# 目前发现的一些问题

## 2026.04.09

1. 本地用docker部署时，启动后在前端执行任务，可以在前端的任务日志里面看到记录，基本和终端里用命令docker compose -f docker-compose.yml logs看到的一致。但是在vps上用docker部署后，前端的任务日志里面看不到任何记录。我怀疑是代码里面写死了localhost，请帮我排查。
2. 本地执行爬取命令时，原作者建议针对初始化的空库需要首先执行全量爬取。我没这样做，我选择了自定义爬取，时间设置为1天内。但是据我观察，后台爬取进程一直在进行，甚至在我关闭容器之前已经爬取了400条话题。帮我看一下代码里关于爬取任务的设置是否有问题。
3. 我在阅读爬取过来的内容时，发现有些球友发布的话题只爬取了内容中开头的一小部分，星球主人发布的话题倒是都能爬取下来。但是两者都没有爬取图片和视频。是不是存在某种过滤机制？还是爬取规则设置的有问题？帮我检查一下。


